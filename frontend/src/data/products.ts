import type { Product } from '../types/types';

// Legacy compatibility export.
// Products are now loaded from the Railway backend API.
export const PRODUCTS: Product[] = [];

export default PRODUCTS;
